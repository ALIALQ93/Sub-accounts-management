# فهرس التدقيق — التقارير المفتوحة فقط

**2026-07-26:** حُذفت كل تقارير `audit-reports/` القديمة (29 ملف) بطلب صريح. تقرير واحد جديد وشامل حل محل تقارير المواد/الفواتير/المناقلات/الأنماط: [`2026-07-26-materials-invoices-audit.md`](audit-reports/2026-07-26-materials-invoices-audit.md) — راجعه أولاً لأي بند يخص هذين القسمين، فهو الأحدث والوحيد المؤكَّد مباشرة من الكود الحالي.

**بقية الأقسام (حسابات، سندات، صلاحيات، مستخدمون، فروع، فترات محاسبية، عملاء/موردون، تقارير محاسبية، POS) فقدت تقاريرها بلا تقرير بديل حتى الآن.** القائمة أدناه تحتفظ بملخص البنود المفتوحة **من الذاكرة فقط** (لا رابط فعلي، الملفات المصدر محذوفة) لحين إعادة تدقيقها بنفس منهجية الملف الجديد.

المنهجية المعتمدة بأي تدقيق لاحق: قراءة الكود الفعلي وتتبّع ترتيب تطبيق الباتشات الحقيقي (`database/build_setup_all.ps1`) — `database/setup_all.sql` هو النسخة المُجمَّعة الفعّالة؛ آخر `create or replace` لأي دالة/تريغر/سياسة هو الفعّال، وليس أول نتيجة بحث نصي.

---

## ✅ مغطّى بتقرير جديد ومؤكَّد (2026-07-26)

- **المواد والفواتير وعلاقتهما** — انظر [`2026-07-26-materials-invoices-audit.md`](audit-reports/2026-07-26-materials-invoices-audit.md). يشمل: مناقلات المخزون، أنماط الفواتير، نموذج الفاتورة، ترحيل، تسعير/تكلفة، RLS.

## 🔴🟠🟡 بنود مفتوحة من تقارير محذوفة (بحاجة تدقيق جديد، غير مؤكَّدة بالكود الحالي)

- تصعيد صلاحيات — `settings.users.manage` / `settings.permissions.manage` → أدمن كامل.
- كشف الحساب — بنود جزئية متبقية (سعر حي لعملة ثالثة، …).
- تعديل سند دفع/قبض مرحّل + مسار عكس ميت.
- قيد يومية مرحّل قابل للتعديل بلا حماية كافية.
- سند التسوية / القيد الافتتاحي — بنود حرجة مفتوحة سابقاً.
- فئات سطور السندات بلا صلاحية DB لـ`auto_post_enabled`.
- RLS مفتوحة على عملاء/موردين/مراكز كلف/عملات (لم يُعاد فحصها ضمن التقرير الجديد — ذاك غطّى فقط `invoices`/`materials`/`warehouses`/`inventory_movements`).
- `bulk_create_accounts()` بلا فحص صلاحية.
- إعداد أولي — أدمن ثانٍ إن انفرغ جدول المستخدمين.
- فروع وإعدادات شركة.
- ميزان مراجعة / سطور مبيعات ومشتريات — تحسينات أو فحص RPC.
- فترات محاسبية.
- POS بيع — بنود جزئية.
- تشتت `post_invoice()` / تريغرات (ملاحظة تاريخية — التقرير الجديد يوثّق مثالاً ملموساً حديثاً لنفس المشكلة، البند 1).

`AUDIT_INVOICES_JOURNAL.md` (جذر المستودع) لا يزال موجوداً ولم يُحذف — بنوده حول FIFO واستلام جزئي أُعيد التحقق منها بشكل مستقل ضمن التقرير الجديد (البندان 5 و7).

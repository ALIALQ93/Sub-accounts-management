-- =============================================================================
-- setup_demo_restaurant.sql — تثبيت كامل + بيانات عرض لمطعم
-- =============================================================================
-- ملف واحد للتجربة أمام العملاء أو بيئة عرض:
--   = database/setup_all.sql  (مخطط النظام كاملاً)
--   + database/demo_restaurant.sql  (بذور مطعم الباب الذهبي)
--
-- الاستخدام: انسخ المحتوى بالكامل → Supabase SQL Editor → Run
--
-- ⚠️ تحذير: يحذف كل البيانات ويعيد البناء من الصفر، ثم يزرع بيانات العرض.
--     يشمل حذف نقاط البيع وBOM والوحدات (كانت ناقصة سابقاً من 00_reset).
--
-- الاستخدام الصحيح: شغّل setup_demo_restaurant.sql كاملاً (وليس demo_restaurant وحدها
-- على قاعدة فيها بيانات قديمة).
--
-- إعادة التوليد:
--     powershell -File database/build_setup_demo_restaurant.ps1
-- =============================================================================
--
-- أولاً — مخطط النظام (داخل setup_all)
-- -----------------------------------------------------------------------------
-- محاسبة · سندات · فروع/فترات · مواد/مخزون · BOM/تصنيع/تفكيك · فواتير · POS
-- تقارير · صلاحيات · Storage — انظر أيضاً setup_all_header.sql
--
-- =============================================================================
--
-- ثانياً — بذور العرض (مطعم الباب الذهبي)
-- -----------------------------------------------------------------------------
-- بيانات أساسية
--   شركة · دليل حسابات · فرعان (MAIN/MANSOUR) · مستودعات (مطبخ/تبريد/منصور)
--   مراكز كلفة · فترة مالية · عملاء/موردون · نقطة بيع POS-HALL
--
-- سلسلة تصنيع مطعم (متعددة المراحل)
--   مواد نيّة (M-LAMB / M-CHICK / M-SPICE…)
--     → محضّرات مخزّنة R-PATTY / R-LAMB-SEAS / R-CHICK-MAR  (finished = أولية للمرحلة التالية)
--     → أطباق قائمة P-BURGER / P-MANSAF / P-CHICK-R / P-GRILL
--   + طقم K-COMBO · تفكيك D-CHICK-WHOLE · تتبّع صلاحية/تسلسلي
--
-- مستندات مرحّلة جاهزة (أرقام DEMO-*)
--   OPEN-YYYY-MAIN     قيد افتتاحي واضح (is_opening_entry)
--   DEMO-PUR-001       مشتريات → غرفة التبريد
--   DEMO-TRO/TRI-COLD  مناقلة تبريد ↔ مطبخ (نفس الفرع)
--   DEMO-MFG-PREP      تصنيع مرحلة 1 (تتبيل)
--   DEMO-MFG-MENU      تصنيع مرحلة 2 (أطباق)
--   DEMO-XFER + TRO/TRI مناقلة بين فرعي الرئيسي والمنصور
--   DEMO-SAL-001       مبيعات صالة نقدية
--   DEMO-RENT-001      قيد إيجار
--
-- =============================================================================
--
-- بعد التشغيل
-- -----------------------------------------------------------------------------
--   1. Authentication → Email/Password
--   2. NEXT_PUBLIC_SUPABASE_URL + PUBLISHABLE_KEY
--   3. /login → أول مستخدم = admin
--   4. راجع: القيود الافتتاحية · الفواتير DEMO-* · بطاقات R-* و BOM · أرصدة المستودعات
-- =============================================================================

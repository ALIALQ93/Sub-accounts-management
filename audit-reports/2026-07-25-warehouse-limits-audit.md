# تدقيق دقيق — وحدة صغيرة: حدود المخزون بالمستودع

> **حالة المعالجة (2026-07-25):** §1 تصحيح «per» ✓ · §2 حفظ متوازٍ مع تجميع أخطاء ✓ · §3 تمييز موروث/خاص بالمستودع ✓.

منهجية: قراءة `web/src/app/materials/warehouse-limits/page.tsx`، `web/src/modules/materials/services/warehouse-material-limits-api.ts`، وجدول `warehouse_material_limits`.

---

## 1. خطأ نصي «per» — ✓ مُغلق

**الإصلاح:** العنوان «حدود المخزون لكل مستودع».

## 2. الحفظ بحلقة تسلسلية — ✓ مُغلق

~~`for` + `await` متتابع، غير ذرّي.~~

**الإصلاح:** `Promise.allSettled` مع رسالة توضح عدد النجاح/الفشل. (ليس RPC ذرّياً واحداً — تحسين كافٍ للعرض المتزامن وتجميع الأخطاء.)

## 3. تمييز موروث vs خاص بالمستودع — ✓ مُغلق

**الإصلاح:** تسمية مصدر القيمة + خلفية مختلفة للصفوف الموروثة من بطاقة المادة.

---

## توصيات مرتّبة حسب الأولوية

### ✓ مُغلق
1. ~~حفظ دفعي/متوازٍ (§2)~~.
2. ~~تصحيح «per» (§1)~~.
3. ~~تمييز بصري (§3)~~.

## ملحق
- `web/src/app/materials/warehouse-limits/page.tsx`
- `web/src/modules/materials/services/warehouse-material-limits-api.ts`

# تدقيق — تناسق واجهات قسم المواد وعلاقات قسم الأصناف بالمشروع

> **حالة المعالجة (2026-07-25):** §1 أزرار البطاقة/المودالات الثلاثة ✓ (مودال الاستيراد الجماعي لا يزال فيه `bg-blue-900` جزئياً) · §2 مودالات `Modal` ✓ · §3 «per» ✓ · §4 حذف `MaterialsNav` ✓ · §5 ترويسة التعديل ✓ · §6 عمود النوع ✓ · §7.3 تعطيل متسلسل ✓ · §7.4 تقارير/تسعير حسب الصنف **مفتوح**.

منهجية: مراجعة بصرية/كودية لكل شاشات قسم المواد مقارنةً بنظام التصميم المشترك، ثم مطابقة سلوك الأصناف مقابل الغرض المُصرَّح به. مكمّل لتدقيق `2026-07-22-materials-warehouses-audit.md`.

---

## 1. انقسام نظام الأزرار — ✓ مُغلق (جزئياً)

~~بطاقة المادة والمودالات الثلاثة كانت بـ`bg-blue-900`/`text-rose-700`.~~

**الإصلاح:** `material-form`، `material-overview-panel`، ومودالات الصنف/الوحدة/المستودع موحّدة مع `btn*` و`--danger`.

**متبقٍ:** `material-bulk-import-modal.tsx` لا يزال يستخدم `bg-blue-900` في بعض الأزرار رغم استخدامه `Modal`.

## 2. مودالات بلا `Modal` المشترك — ✓ مُغلق

**الإصلاح:** الصنف والوحدة والمستودع تستخدم `Modal`.

## 3. أخطاء نصية «per» — ✓ مُغلق

**الإصلاح:** حدود المخزون + التسوية المجمّعة.

## 4. `MaterialsNav` ميت — ✓ مُغلق

**الإصلاح:** حُذف الملف وكل الاستيرادات.

## 5. تفاوت الترويسات — ✓ مُغلق (صفحة التعديل)

**الإصلاح:** صفحة تعديل المادة موحّدة. (حدود المخزون/التسوية المجمّعة تبقيان `mb-1` + وصف — مقصود لوجود فقرة توضيح.)

## 6. قائمة المواد بلا تمييز تجميعي — ✓ مُغلق

**الإصلاح:** عمود «النوع».

## 7. علاقات الأصناف

### 7.1 حسابات على مستوى الصنف — لا حاجة ✅
### 7.2 ترقيم الكود — منفَّذ ✅
### 7.3 تعطيل الأب → الأبناء — ✓ مُغلق

**الإصلاح:** `patch_material_category_cascade_deactivate.sql` + تأكيد واجهة.

### 7.4 تقارير / POS / تسعير — مفتوح جزئياً

| الاستخدام | الحالة |
|---|---|
| تقارير مبيعات/مشتريات/COGS حسب الصنف | ⚠️ مفتوح |
| فلترة POS حسب الصنف | ✅ منفَّذ خلفياً |
| تسعير/خصم حسب الصنف | ❌ غير موجود — تصميم مستقبلي |

---

## توصيات مرتّبة حسب الأولوية

### ✓ مُغلق
1. ~~تعطيل متسلسل للصنف الأب (§7.3)~~.
2. ~~توحيد أزرار البطاقة والمودالات الثلاثة (§1)~~.
3. ~~تحويل المودالات الثلاثة لـ`Modal` (§2)~~.
4. ~~تصحيح «per» (§3)~~.
5. ~~حذف `MaterialsNav` (§4)~~.
6. ~~ترويسة التعديل + عمود النوع (§5–6)~~.

### 🔵 مفتوح
7. توحيد أزرار `material-bulk-import-modal` مع `btn*`.
8. تجميع تقارير المبيعات/المشتريات/COGS حسب الصنف (§7.4).
9. تسعير/خصم حسب الصنف — تصميم من الصفر إن رُغب (§7.4).

## ملحق
- `database/patch_material_category_cascade_deactivate.sql`
- `web/src/modules/materials/components/material-bulk-import-modal.tsx` — متبقٍ جزئياً
